'''As an exercise, write a compare function takes two values, x and y, and returns 1 if x > y,
0 if x == y, and -1 if x < y.'''

def compare_function(x,y):
    if x > y:
        h = 1
        return h
    elif x == y:
        return 0
    elif x < y:
        return -1

compare_function(4,8)
